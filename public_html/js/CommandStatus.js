/* jshint -W089*/
/*global define, console, document, window, navigator */
define([

], function (

) {
    'use strict';

    var CommandStatus = function () {

    };

    CommandStatus.DONE = 0;
    CommandStatus.CONTINUE = 1;

    return CommandStatus;
});
